# AI 风格化滤镜的额外依赖（仅在使用 /api/ai-style 时需要）。
#
# 建议安装顺序：
#   1) 先按官方说明装匹配本机 CUDA 的 torch / torchvision
#      https://pytorch.org/get-started/locally/
#      （CUDA 12.1 示例：
#         pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121 ）
#   2) 然后再安装本文件其余依赖：
#         pip install -r requirements-ai.txt
#
# 首次调用 /api/ai-style 时会从 HuggingFace 下载约 7GB 的 sdxl-turbo 权重。
# 推荐 NVIDIA GPU + ≥8GB VRAM。CPU 也能跑，单张 1-3 分钟。

diffusers>=0.27.0
transformers>=4.39.0
accelerate>=0.27.0
safetensors>=0.4.0
Pillow>=10.0.0
